### MathML Intent Editor

- high contrast reds for delete
- high contrast saturated green for added
- and borders for WCAG 2.0 - 
- Additional notations - make rendering be on a full line so that rendering matches on the left panel.
- also add an info icon on the left for edited/added/deleted 
- cap the title at a certain length to avoid overflow
- add "open" to name of the mathml-intent-editor
- convert the thead for language(en) to a dropdown for language select
- open PR CI checks 
   - no overlap with Core list
   - no duplicates in main list
   - disable the CI warnings for YAML length.
- Add FAQ explaining what permissions are required by the GH signin to dispell fears.
