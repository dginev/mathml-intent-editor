{
  "name": "mathml-intent-service",
  "private": true,
  "type": "module",
  "version": "0.2.0",
  "description": "Thin OAuth code→token exchange for the MathML Intent Open Editor (deployed on latexml.rs).",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.1"
  }
}
