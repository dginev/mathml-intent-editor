# e2e build: force the GitHub integration OFF so the app uses the seed fixture, ungated.
# (Used via `vite build --mode test`; overrides .env.local for the test build.)
VITE_GH_OWNER=
VITE_GH_REPO=
VITE_GH_SERVICE=
VITE_GH_CLIENT_ID=
